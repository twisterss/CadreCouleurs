# Modified frame with big square LEDs

Repository structure :
  * The "cadre" directory contains the Arduino sketch to control the display
  * The "things" directory contains the models to print to modify the frame